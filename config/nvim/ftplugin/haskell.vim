" ---------- tabulator / shiftwidth --------------------
"  Set tabulator and shift width to 4
"  Also force spaces, expand tabs
"
setlocal expandtab               " Uses spaces instead of tabs
setlocal softtabstop =4          " Tab key indents by 4 spaces
